# Universal Polyglot Refactoring & Architectural Guidelines

You are an expert Principal Software Architect. Your objective is to refactor this codebase to maximize modularity, readability, and testability while maintaining strict functional correctness and zero regressions.

## 🏛️ Core Architectural Philosophies (Language-Agnostic)
- **Clean Architecture & The Dependency Rule:** Business rules and core execution logic must be isolated from external frameworks, databases, network protocols, and side effects. Dependencies must only point inward toward core business intent. Use the Ports and Adapters (Hexagonal) pattern to abstract I/O.
- **Information Hiding (Deep Modules):** Expose simple, minimal external interfaces. Hide complex implementation details internally. Prioritize locality of behavior over deeply nested, highly fragmented micro-files that lead to tight coupling.
- **Contract-Driven Design:** Before refactoring any scope, explicitly identify or define the data types, schemas, inputs, and outputs crossing that boundary. Ensure those contracts are strictly enforced post-refactor.

## 🔧 Technology-Specific Guardrails

### 1. Smart Contracts & Web3 (Solidity, Foundry, Hardhat)
- **Security & Efficiency:** Adhere strictly to the ConsenSys Solidity Best Practices. Keep functions short, optimize gas costs (e.g., caching storage variables in memory), and strictly check access controls (`onlyOwner`, custom modifiers).
- **State Modifications:** Enforce the Checks-Effects-Interactions pattern globally to prevent reentrancy vulnerabilities.
- **Tooling Verification:** Every architectural shift or optimization must be compiled and validated using `forge test` or `npx hardhat test`. Ensure fuzzing or invariant tests do not break.

### 2. Frontend & Full-Stack Ecosystems (TypeScript, React, Next.js, Vue)
- **Separation of Concerns:** Separate pure UI rendering logic from data fetching, state management, and business hooks.
- **Type Rigor:** Eliminate all instances of `any`. Utilize strict interfaces, generic type parameters, and schema validation libraries (like Zod or ArkType) to parse untrusted network I/O or form inputs.
- **State Locality:** Keep state as local as possible to avoid unnecessary global context re-renders.

### 3. Backend Services & APIs (Node.js, Python, Go, Java)
- **Statelessness & Scalability:** Keep application layers stateless. Offload state to external databases or caches.
- **Robust Error Handling:** Never swallow exceptions. Implement centralized middleware for error logging and map internal execution errors cleanly to HTTP/gRPC status codes.
- **Boundary Validation:** Enforce structural type hints or data models (e.g., Pydantic in Python, NestJS DTOs) at the controller/gateway boundary.

### 4. Infrastructure & Scripting (Bash, Shell, Python Scripts)
- **Idempotency & Safety:** Shell scripts must execute safely using `set -euo pipefail`. Localize variable scopes explicitly with `local`.
- **Modularity:** Break down monolithic automation scripts into testable, discrete functions. Validate syntax and anti-patterns against tools like `shellcheck`.

## 🔄 Verification, Linting, & Quality Gate Loop
When asked to perform a refactor, you must utilize your local terminal access to execute this strict continuous-verification loop:
1. **Establish Baseline:** Execute the project's linter, compiler, and test runner before modifying any code to ensure a passing baseline.
2. **Incremental Scoping:** Refactor in small, atomic batches. Do not alter more than 150 lines of code across any given file in a single step.
3. **Automated Verification:** Run local verification tools (e.g., `forge test`, `npm run lint`, `pytest`, `tsc --noEmit`, `shellcheck`) after every single atomic change.
4. **Automated Rollback:** If a verification gate fails and a clean patch cannot be resolved immediately, use git tools to revert the changes to that specific file and attempt a different, safer structural pattern.

---

## Project contracts

Measured 2026-08-29 on this machine. Every value
came from a command that was actually run; anything not established says `unknown`. See the
`project-contracts` skill for what each field is for and how to rebuild this.

```
Verification
  behaviour: uv run pytest -m "not integration"   # exit 0 on four runs: 1707 passed,
             1 skipped, 1 deselected every time. 331-986s: the spread is machine load,
             not the suite. Budget 17 min to be safe; an idle machine does it in 6.
    covers:  98 of 107 modules under src/engram/ are named by a test import. tests/cli/
             drives the Typer app in-process; init, serve, daemon, doctor, delete,
             team-vault, move-thought and consolidate also get a hermetic subprocess
             smoke against the installed binary.
    blind:   src/engram/cli/migrate.py (171 lines) - nothing imports it and no test runs
             `migrate-from-open-brain`; the pipeline beneath it is covered, the CLI
             wiring is not. `engram reindex` smoke asserts only the lock-refusal exit 2,
             so it would pass with the rebuild gutted; summarize/synthesize smoke is
             --help plus one bad-UUID refusal. The real FastEmbed download is the lone
             `integration`-marked test, deselected here and in CI, so the embedding
             round-trip never runs. Linux SO_PEERCRED auth is the one skip on macOS. The
             GPG team tests are skipif-gated and vanish silently on a box without gpg.
             Multi-machine sync is simulated against local remotes only.
  shape:     uv run ruff format --check .   # exit 0, 277 files
             uv run ruff check .            # exit 0
             uv run mypy                    # exit 0, 275 files, strict
             ./.githooks/verify-gates.sh    # exit 0, 5 locked entries + live probes
             ./.githooks/planning-vocab-ratchet.sh --check .  # exit 0, 490/490 baselined
  sensitivity: real, and scoped. tests/test_gates.py (21 planted-defect cases) and
             verify-gates.sh each mutate a copy of .githooks/ and require the gate to go
             red; a scanner rewritten to `exit 0` and honestly re-hashed fails. Both
             green 2026-08-29. Nothing equivalent covers pytest/ruff/mypy over src/ - no
             mutation harness, so green there means "did not object".
  not run here: uv build plus the wheel and console-script smoke (CI-only jobs; they
             write dist/). The coverage floor (fail_under = 80) bites only on --cov.

Provenance
  authored:   src/**, tests/**, bench/**, docs/**, .github/workflows/ci.yml,
              .githooks/{verify-gates.sh,revendor.sh,vendor.lock,README.md}
  vendored:   the other five files in .githooks/ (pii-scan.sh, pii-patterns.conf,
              planning-vocab-{scan,ratchet}.sh, planning-vocab-patterns.conf), pinned by
              sha256 in .githooks/vendor.lock. Take a change with
              ./.githooks/revendor.sh --source <dir>, never an in-place edit.
              last synced: unknown - the lock records content, never a date or a revision.
  generated:  uv.lock (uv), dist/ (uv build, gitignored), .planning-vocab-baseline
              (planning-vocab-ratchet.sh --write). No code generator, no templates, and
              no tracked symlinks (git ls-files -s reports zero mode-120000 entries).
  intentional:LICENSE line 189 carries the maintainer's name - the sanctioned attribution
              exception, so a whole-tree pii-scan exits 1 on it by design (CI scans only
              changed files; tests/test_gates.py excludes LICENSE explicitly).
              .planning-vocab-baseline freezes 490 accepted findings; the ratchet fails
              on additions only, so do not "clean" it.
              _ALLOWED_DIRECT_WRITES in tests/utils/test_atomic_write.py pins exact
              per-file counts of non-atomic writes - a shrunk count fails too.
              onnxruntime>=1.17,<1.21 buys Intel-Mac wheels and sets the 3.11-3.13
              ceiling; unpinning silently drops a supported target.
              errors.py keeps noun-form exception names (ruff N818 ignored) because the
              public MCP error_code map spells them that way.
              Each vendor.lock line ends `# pii-allow: sha256`: a sha256 prefix matches
              the 40-hex GPG-fingerprint pattern.
  liveness:   THIS TREE IS LIVE. The `engram` on PATH is a uv-tool **editable** install of
              this checkout, and .venv holds a second editable install; an edit to
              src/engram/ changes the installed binary for every process started after it.
              Running as of this measurement: one `engram daemon` (started 2026-08-27,
              serving a vault in a separate private repo) plus ten `engram serve` proxies.
              They hold the code they imported at spawn - a source edit does not reach
              them until `engram daemon stop` and a restart, then `engram doctor`.
  local-only: .git/info/exclude (machine-local, uncommitted) hides .scrub/ and
              .claude/RESUME.md here. A fresh clone sees both as untracked.

Authority
  No surface states one total order; these are the claims the surfaces make.
  1. the seven pinned invariants in CLAUDE.md, "DO NOT VIOLATE" - markdown beats SQLite,
     portability=block never reaches an LLM, two-layer enforcement, stable v1.x wire format
  2. the gates above, as .github/workflows/ci.yml runs them
  3. CLAUDE.md for conventions - CONTRIBUTING.md: "CLAUDE.md is authoritative for the
     pinned invariants ... Read it before changing code"
  4. .githooks/README.md for that directory; CONTRIBUTING.md names it the reference
  5. this repo over the maintainer's unpublished planning spec - CLAUDE.md: "this repo is
     authoritative for what engram does"
  6. README.md and docs/ explain, docs/adr/ (9) records why; neither overrides 1-5

Boundary
  MCP I/O types      src/engram/models/mcp.py - 14 Pydantic models, extra="forbid" in,
                     extra="ignore" out; additive-only for v1.x
  markdown schema    src/engram/models/{frontmatter,thought}.py
  config             src/engram/config/models.py, loaded in five layers by loader.py
  daemon <-> proxy   src/engram/daemon/protocol.py - NDJSON JSON-RPC, 16 MiB frame cap
  bundle on disk     src/engram/bundle/format.py - manifest.json, schema_version 1
  error codes        src/engram/errors.py; the error_code map IS public wire format
  doctor checks      src/engram/diagnostics/check_codes.py
  provider seams     src/engram/embedding/protocol.py, src/engram/llm/protocol.py
  SQL                src/engram/storage/sqlite_queries.py, ?-placeholders only
  new modules: one directory per subsystem under src/engram/<subsystem>/, mirrored by
    tests/<subsystem>/test_<module>.py. Established - 16 subsystem dirs already follow it,
    and tests/ mirrors every one. Only __init__, __main__, errors and logging sit at the
    package root; do not add a fifth.

  Adding a CLI subcommand, in order. Steps 2 and 4 fail silently when skipped:
    1. write src/engram/cli/<name>.py exposing register(app)
    2. in src/engram/cli/__init__.py add BOTH the import and the _<name>_cmd.register(app)
       call - two separate edit sites, and an import without the call is a no-op
    3. wire it into engram doctor / build_multivault_server in the same change; CLAUDE.md
       records deferred wiring as a repeat failure here
    4. add a hermetic subprocess smoke test - handler tests do not see wiring bugs

  Local hooks: only pre-commit is installed (.git/hooks/pre-commit, via the pre-commit
    framework: ruff, ruff-format, mypy, pii-scan --staged). core.hooksPath is unset and
    there is no pre-push hook, so verify-gates.sh and the vocabulary ratchet run in CI
    only. Wire the local half with `uv run pre-commit install`.
```
